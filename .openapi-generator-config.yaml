# OpenAPI Generator Configuration for hackagent
# This file configures how openapi-python-client generates the API client

# Project metadata
project_name_override: "hackagent"
package_name_override: "hackagent"

# Generation options
use_path_prefixes_for_title_model_names: false
field_prefix: ""
